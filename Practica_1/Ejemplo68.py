import numpy as np
a = np.array([1, 2, 3, 4, 5])# se crea el arreglo con el rango
b = np.square(a)#se calcula el cuadrado de los elemntos
c = np.sqrt(a) # se calcula la raiz cuadrada de los elemntos
d = np.log(a) # se calcula el logitipo natural de los elementos
print(b) # se imprime el nuevo arreglo
print(c) # se imprime el nuevo arreglo
print(d) # se imprime el nuevo arreglo