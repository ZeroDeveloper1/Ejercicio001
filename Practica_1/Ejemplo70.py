import numpy as np
a = np.array([1, 2, 3, 4, 5])# se crea el arreglo con el rango
# se crea 5 elementos
g = np.sum(a) #se calcula la suma de los elementos
h = np.sum(a) #se calcula el promedio de los elementos
i = np.max(a)#se calcula el maximo de los elementos
print(g) # se imprime el resultado 
print(h) # se imprime el resultado
print(i) # se imprime el resultado