import numpy as np
#se crea una arrgelo de 5 elemntos
a = np.array([0, np.pi/2, np.pi, 3*np.pi/2, 2*np.pi])
j = np.sin(a) #se calcula la seno de los elementos
k = np.cos(a) #se calcula el coseno de los elementos
l = np.tan(a)#se calcula el tagente de los elementos
print(j) # se imprime el resultado 
print(k) # se imprime el resultado
print(l) # se imprime el resultado