import numpy as np
# se importa la funcion norm desde el modulo numpy. linalg
from numpy.linalg import norm

a = np.array([1, 2, 3]) # se crea un vector de 3 elemntos
b = np.array ([[1,2],[3,4]]) # se crea un matriz de 2x2 elementos
e = norm(a)# se calculo la norma euclidiana del vector
E = nomr(A, np.inf) #se calcula la norma infinito de la matriz
print(e)
print(E)

